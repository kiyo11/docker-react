sudo: required
services:
  - docker

before_install:
  - docker build -t douglum/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true douglum/docker-react npm run test -- --watchAll=false